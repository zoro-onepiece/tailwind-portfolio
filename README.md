1.run npm i to install node modules 
2.then type these commands in terminal:

# Terminal window 1 – compile & watch Tailwind/CSS
npm run css -- --watch

# Terminal window 2 – start browser-sync
npm run server-reload


3.Make sure you have live server extension intalled.Then right click index.html and clicl open/start with live server to prview the files locally.